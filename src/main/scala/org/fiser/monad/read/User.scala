package org.fiser.monad.read

case class User(id: Int,
                username: String,
                firstName: String,
                lastName: String,
                email: String,
                supervisorId: Int)
